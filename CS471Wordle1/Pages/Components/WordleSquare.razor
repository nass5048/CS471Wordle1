@using WordleAPI.Classes
@using WordleAPI
@using CS471Wordle1.Pages.Components
<div class="wordle-square" style="background-color:@GetColor()">
	<div class="letter">@Letter</div>
</div>
<style>
	.letter{
		color: white;
		font-size: xx-large;
	}

	.wordle-square {
		text-align: center;
		width: 50px;
		height: 50px;
		display: inline-flex;
		justify-content: center;
	}

</style>

@code {
	// Displays letter in the tile
	[Parameter]
	public char Letter { get; set; }

	// Displays color state of the title
	[Parameter]
	public ResponseColor Color { get; set; }

	// Displays color associated with the state of the tile
	private string GetColor()
	{
		return Color switch
		{
			ResponseColor.green => "green",
			ResponseColor.yellow => "goldenrod",
			ResponseColor.grey => "grey",
			_ => "black",
		};
	}

}
