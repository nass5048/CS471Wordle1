@using WordleBackend.Enums
@using WordleAPI.Classes
@using CS471Wordle1.Pages.Components
@page "/game/{gameModeParam}/{challengeParam}/{useAI:bool}"

<Wordle challenge="challenge" gameMode="gameMode" UseAI="useAI"/>
@code {
    // Receives selected challenge mode
    [Parameter]
    public string challengeParam { get; set; }

    // Receives selected game mode
    [Parameter]
    public string gameModeParam { get; set; }

    // Determines if AI opponent is enabled by user
    [Parameter]
    public bool useAI { get; set; }

    public GameMode gameMode => Enum.Parse<GameMode>(gameModeParam);
    public Challenge challenge => Enum.Parse <Challenge>(challengeParam);
}